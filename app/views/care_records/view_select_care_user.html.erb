<div class="flex flex-col items-center justify-start min-h-screen bg-orange-50 px-4 pt-5">
  <div class="text-center text-4xl font-bold mb-8">
    <h1>記録を見る人を<br>選んでください</h1>
  </div>

  <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 max-w-2xl">
    <% if @care_users.present? %>
      <% @care_users.each do |care_user| %>
        <%= link_to care_records_path(care_user_id: care_user.id), class: "w-75 flex justify-center items-center px-10 py-6 text-3xl text-center text-white bg-orange-500 hover:bg-orange-300 font-bold rounded-lg" do %>
          <% if @unread_care_user_ids.include?(care_user.id) %>🔵 <% end %><%= care_user.name %>
        <% end %>
      <% end %>
    <% else %>
      <div class="col-span-full text-center text-4xl mb-5 font-bold">見守り家族の登録は<br>ありません</div>
    <% end %>
  </div>

  <div class="mt-4 grid grid-cols-1 gap-4 max-w-2xl">
    <%= link_to "戻る", homes_path, class: "w-75 flex justify-center items-center px-10 py-6 text-3xl text-center text-white bg-gray-500 hover:bg-gray-300 font-bold rounded-lg" %>
  </div>
</div> 